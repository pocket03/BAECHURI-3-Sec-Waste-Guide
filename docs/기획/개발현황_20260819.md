# 배추리 매니저 — 개발 현황 및 다음 작업

> 최초 작성: 2026-08-19 / 최종 갱신: 2026-08-20 / 관련 문서: [PRD](배추리매니저-PRD_20260812.md)

---

## 1. 지금까지 작업한 것

### MVP 핵심 기능 (PRD Must/Should 기준)
- 세입자 화면: 언어 선택(`/guide`) → 오늘의 배출 신호등(또는 집주인이 설정한 공지 배너)·FAQ 버튼형 챗·품목 30종 아이콘 검색(`/search`) → 결과 카드(`/result`, 품목/장소/공지 쿼리 파라미터로 분기). 로그인 불필요
- 알림톡 템플릿 5개(쓰레기 배출 2 · 퇴실 청소 1 · 원룸 공지사항 2) → 한/영/중/베 4개국어 조합 메시지 자동 생성, 카카오톡 발송용 전체 복사
- 데이터: 품목 30종, UI 문자열, 알림톡 템플릿 전부 4개 언어로 작성 완료

### 배포
- GitHub 저장소 2곳(개인용 `BAECHURI_404`, 팀 저장소 `BAECHURI-3-Sec-Waste-Guide`)에 코드 반영
- Vercel 배포 완료: `https://baechuri-404.vercel.app`

### 집주인 로그인 + 건물별 관리 구조 (멀티테넌트)
- **로그인 위치가 `/`(첫 화면) 자체로 통합됨** — 별도 `/admin` 없음. 비로그인 시 `/login`으로 리다이렉트, 로그인하면 그 계정 소유의 건물 데이터만 관리
- Supabase Auth 기반 이메일/비밀번호 로그인, 공개 회원가입 없음(계정은 Supabase 대시보드에서 직접 생성)
- 공지사항·세입자 목록·FAQ·건물 설정 모두 `landlord_id`로 소유자 구분(RLS), 세입자용 조회는 로그인 없이 공개
- **우리 건물 QR·링크**(`/guide?b=<집주인ID>`)로 들어온 세입자에게만 그 집주인이 설정한 FAQ·공지·신호등이 노출됨

### 집주인이 직접 관리하는 콘텐츠
- **공지사항 추가·수정·삭제 + 저장 즉시 문자 발송**: 공지 작성 시 Google Translate로 3개 언어 자동 번역 → Supabase에 저장 → 등록된 세입자 전체에게 바로 문자(SMS) 발송까지 한 번에 처리. 공지별 세입자용 링크·QR도 자동 생성(`/result?custom=<id>`)
- **FAQ 관리**: 집주인이 직접 질문/답변 작성 → 자동 번역 → 그 건물 QR로 들어온 세입자에게만 노출(비어있으면 기본 FAQ 4개로 대체)
- **오늘의 배출 신호등 · 공지 배너**: 건물별 재활용 배출 요일 설정 가능. 공지 배너를 써두면 신호등 자리가 배너로 대체되어 표시(두 기능이 한 칸을 공유)
- **세입자 전화번호 관리**: 전화번호 + 언어 등록, 문자 발송 대상 목록으로 사용

### 문자(SMS) 알림 — 실제 발송
- Solapi 연동 완료(발신번호 등록·API 키 적용), 배포 사이트에서 실발송 테스트 확인됨
- 세입자가 등록한 언어에 맞는 번역본이 발송됨
- 환경변수 3개(`SOLAPI_API_KEY`/`SOLAPI_API_SECRET`/`SOLAPI_SENDER_NUMBER`) 중 하나라도 비어 있으면 자동으로 Mock(콘솔 로그만) 모드로 전환되는 안전장치 유지

### 디자인
- 집주인 대시보드(로그인 후 `/`)를 카드 나열식에서 **탭 구조**(발송 / 공지사항 / FAQ / 세입자 / 건물 설정)로 재구성. 큰 화면에서는 2단 배치로 여백 활용 — 반응형 문제도 같이 해결됨
- 세입자 화면(`/guide`, `/search`, `/result`)은 아직 손대지 않음(다음 작업으로 보류)

### 보안
- 구글 번역 키는 서버 환경변수로만 사용(클라이언트 노출 없음), `.env*`는 전부 `.gitignore` 처리, Vercel 환경변수로 프로덕션 키 관리
- 공지사항 문자 발송 API에 소유권 검증 추가(다른 집주인 공지로 발송 시도 차단)

---

## 2. 남은 작업

| 기능 | 내용 | 비고 |
|---|---|---|
| **세입자 화면 디자인 폴리싱** | `/guide`, `/search`, `/result`의 카드 스타일·여백·타이포그래피 통일 | 오늘은 집주인 대시보드만 개편, 세입자 화면은 보류 |
| **세입자 → 집주인 문의 요청** | 세입자가 집주인에게 궁금한 점이나 요청 사항을 보낼 수 있는 기능 | 로그인 없는 세입자가 보낸 내용을 집주인이 로그인 후 확인하는 구조가 될 것으로 예상. 저장 위치(DB), 알림 방식 결정 필요(현재 논의 중단 상태) |
| (선택) **피그마로 디자인 추가 작업** | html.to.design 같은 플러그인으로 배포 사이트를 피그마에 가져와 팀원이 추가로 다듬는 흐름 | 코드→피그마 자동 동기화는 안 되므로, 피그마에서 바꾼 내용은 다시 코드로 옮겨야 함 |

---

## 3. 참고 — 현재 계정/환경변수 및 테이블

- Supabase 프로젝트: `https://ntoilgkmqqptuvhagfsu.supabase.co`
  - 테이블: `notices`, `tenants`, `faqs`, `building_settings` (전부 `landlord_id`로 소유자 구분, RLS 적용)
- Google Cloud Translation API: 번역 기능용, Vercel 환경변수 `GOOGLE_TRANSLATE_KEY`로 등록됨
- Solapi (문자 발송): 발신번호 등록 완료, 실제 발송 확인됨. Vercel 환경변수 `SOLAPI_API_KEY`, `SOLAPI_API_SECRET`, `SOLAPI_SENDER_NUMBER` 등록됨
- 실제 키 값은 이 문서에 적지 않음 — Vercel 프로젝트 환경변수 및 `.env.local`(로컬, git 미포함)에서 관리
- 테이블 생성 SQL: [notices_table.sql](notices_table.sql) · [tenants_table.sql](tenants_table.sql) · [multi_tenant_migration.sql](multi_tenant_migration.sql) · [building_settings_table.sql](building_settings_table.sql) · [building_banner_migration.sql](building_banner_migration.sql)
